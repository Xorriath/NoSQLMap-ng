version: "3"
services:
  nosqlmap:
    image: nosqlmap-ng:latest
    build:
      # Build from the repo root so the local package is installed.
      context: ..
      dockerfile: docker/Dockerfile
    stdin_open: true
    tty: true
